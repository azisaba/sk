on join:
    wait a tick
    console command "/mv tp %player% lobby"

command /hub:
    executable by: players
    aliases: /lobby, /spawn
    trigger:
        send "&bロビーにテレポートしています…"
        console command "/mv tp %player% lobby"
