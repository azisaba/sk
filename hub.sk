on join:
    wait a tick
    console command "/mv tp %player% lobby"

command /hub:
    executable by: players
    aliases: /lobby, /spawn
    trigger:
        send "&bHubにテレポートしています…"
        console command "/mv tp %player% lobby"
